<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <bean id="kunpengDataSource" class="com.alibaba.druid.pool.DruidDataSource" init-method="init"
          destroy-method="close">
        <property name="driverClassName" value="${jdbc.kunpeng.database.driver}"/>
        <property name="url" value="${jdbc.kunpeng.database.url}"/>
        <property name="username" value="${jdbc.kunpeng.database.username}"/>
        <property name="password" value="${jdbc.kunpeng.database.password}"/>
        <property name="defaultAutoCommit" value="false"/>

        <!-- 配置初始化大小、最小、最大 -->
        <property name="initialSize" value="3"/>
        <property name="minIdle" value="1"/>
        <property name="maxActive" value="50"/>

        <!-- 配置获取连接等待超时的时间 -->
        <property name="maxWait" value="60000"/>

        <!-- 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒 -->
        <property name="timeBetweenEvictionRunsMillis" value="60000"/>

        <!-- 配置一个连接在池中最小生存的时间，单位是毫秒 -->
        <property name="minEvictableIdleTimeMillis" value="300000"/>

        <!--<property name="validationQuery" value="SELECT 'x'" />-->
        <property name="testWhileIdle" value="true"/>
        <property name="testOnBorrow" value="false"/>
        <property name="testOnReturn" value="false"/>

    </bean>

    <bean id="kunpengSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="configLocation" value="classpath:mybatis/mysql-sqlmap-config.xml"/>
        <property name="mapperLocations" value="classpath:mybatis/mappers/kunpeng/*.xml"/>
        <property name="typeAliasesPackage" value="cn.tongdun.kunpeng.share.dataobject"/>
        <property name="dataSource" ref="kunpengDataSource"/>
    </bean>

    <bean id="kunpengSqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="kunpengSqlSessionFactory"/>
    </bean>

    <bean name="kunpengMapperScannerConfigurer" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="cn.tongdun.kunpeng.api.infrastructure.persistence.mybatis.mappers.kunpeng"/>
        <property name="sqlSessionFactoryBeanName" value="kunpengSqlSessionFactory"/>
    </bean>




    <bean id="forsetiDataSource" class="com.alibaba.druid.pool.DruidDataSource" init-method="init"
          destroy-method="close">
        <property name="driverClassName" value="${jdbc.forseti.database.driver}"/>
        <property name="url" value="${jdbc.forseti.database.url}"/>
        <property name="username" value="${jdbc.forseti.database.username}"/>
        <property name="password" value="${jdbc.forseti.database.password}"/>
        <property name="defaultAutoCommit" value="false"/>

        <!-- 配置初始化大小、最小、最大 -->
        <property name="initialSize" value="3"/>
        <property name="minIdle" value="1"/>
        <property name="maxActive" value="50"/>

        <!-- 配置获取连接等待超时的时间 -->
        <property name="maxWait" value="60000"/>

        <!-- 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒 -->
        <property name="timeBetweenEvictionRunsMillis" value="60000"/>

        <!-- 配置一个连接在池中最小生存的时间，单位是毫秒 -->
        <property name="minEvictableIdleTimeMillis" value="300000"/>

        <!--<property name="validationQuery" value="SELECT 'x'" />-->
        <property name="testWhileIdle" value="true"/>
        <property name="testOnBorrow" value="false"/>
        <property name="testOnReturn" value="false"/>

    </bean>

    <bean id="forsetiSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="configLocation" value="classpath:mybatis/mysql-sqlmap-config.xml"/>
        <property name="mapperLocations" value="classpath:mybatis/mappers/forseti/*.xml"/>
        <property name="typeAliasesPackage" value="cn.tongdun.kunpeng.api.infrastructure.persistence.dataobj"/>
        <property name="dataSource" ref="forsetiDataSource"/>
    </bean>

    <bean id="forsetiSqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="forsetiSqlSessionFactory"/>
    </bean>

    <bean name="forsetiMapperScannerConfigurer" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="cn.tongdun.kunpeng.api.infrastructure.persistence.mybatis.mappers.forseti"/>
        <property name="sqlSessionFactoryBeanName" value="forsetiSqlSessionFactory"/>
    </bean>

</beans>